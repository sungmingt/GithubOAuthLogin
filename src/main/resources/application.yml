spring:
  profiles:
    include: oauth
  datasource:
    url: jdbc:h2:mem:test
    name: sa
    password: test
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
